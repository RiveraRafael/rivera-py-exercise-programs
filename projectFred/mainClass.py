import rooms as rm

rooms:str = ["1", "2", "3", "4"]

bots = ["B", "C", "F"]

roomsNew = []

roomsNew.append( rm.rooms(1, "B"))

print(rm.rooms.readNumber(0))

for x in range(len(rooms)):
    print("Room " + rooms[x] + ":")